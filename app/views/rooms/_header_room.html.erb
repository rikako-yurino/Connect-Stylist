<header class='top-page-header'>
  <div class='search-bar-contents'>
  <%= link_to image_tag("ps-logo-color.png", class:"ps-icon"), "/" %>
  <% if user_signed_in? %>
      <ul class='lists-right'>
          <li><%= link_to "チャットを退出する", reservation_room_path, method: :delete, class: "logout"  %></li>
      </ul>
  <% elsif stylist_signed_in? %>
      <ul class='lists-right'>
          <li><%= link_to "チャットを退出する", stylists_path,  class: "logout"  %></li>
      </ul>
  <% end %>
</div>